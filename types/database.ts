/**
 * Placeholder para os tipos gerados a partir do schema real.
 *
 * Depois de aplicar a migration no projeto Supabase, gerar com:
 *   npx supabase gen types typescript --project-id <PROJECT_ID> > types/database.ts
 *
 * Até lá, `Database` fica como `any` para não travar o build —
 * substituir assim que o projeto Supabase estiver criado.
 */
// eslint-disable-next-line @typescript-eslint/no-explicit-any
export type Database = any;
